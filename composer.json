{
    "name": "furbo/renderer",
    "type": "library",
    "version": "1.0.0",
    "description": "Server side rendering of website requests. Rendered output includes content loaded asynchronously over javascript, regardless of the technology used (like react, vue or similar). Can be used to prerender single page applications for good search engine indexing or to generate pgs from URL's.",
    "keywords": [
        "SPA",
        "Server Side Rendering",
        "Prerender"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.0",
        "guzzlehttp/guzzle": "^6.3"
    },
    "require-dev": {

    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "Furbo\\": "src/"
        }
    },
    "autoload-dev": {

    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "scripts": {

    }
}
